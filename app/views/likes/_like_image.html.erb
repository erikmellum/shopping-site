<%= form_for([@image, @like], class: 'inline') do |f| %>
  <% if @like.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@like.errors.count, "error") %> prohibited this like from being saved:</h2>

      <ul>
      <% @like.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="actions inline">
   <%= button_tag(type: 'submit', class: "inline nobuttonstyle") do %>
     <i class="fa fa-thumbs-up">Like</i> 
   <% end %>
  </div>
<% end %>
